/// ElevenLabs API Secrets - EXAMPLE FILE
/// 
/// 📋 SETUP INSTRUCTIONS:
/// 1. Copy this file to 'elevenlabs_secrets.dart' (remove .example)
/// 2. Replace the placeholder values with your actual credentials
/// 3. Never commit elevenlabs_secrets.dart to git (it's in .gitignore)
///
/// 🔑 Get your credentials from: https://elevenlabs.io/app/conversational-ai
class ElevenLabsSecrets {
  // API Credentials
  static const String apiKey = 'REPLACE_WITH_YOUR_API_KEY';
  static const String agentId = 'REPLACE_WITH_YOUR_AGENT_ID';
  
  // Development credentials (optional - for testing)
  static const String devApiKey = 'YOUR_DEV_API_KEY_HERE';
  static const String devAgentId = 'YOUR_DEV_AGENT_ID_HERE';
  
  /// Validate if production credentials are set
  static bool get isConfigured {
    return apiKey != 'REPLACE_WITH_YOUR_API_KEY' && 
           apiKey.isNotEmpty &&
           agentId != 'REPLACE_WITH_YOUR_AGENT_ID' &&
           agentId.isNotEmpty;
  }
  
  /// Validate if development credentials are set
  static bool get isDevConfigured {
    return devApiKey != 'YOUR_DEV_API_KEY_HERE' && 
           devApiKey.isNotEmpty;
  }
}

